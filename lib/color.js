
const color = (text, color) => {
    return text; // Simplificado: no aplica color real en consola
};

const bgcolor = (text, bgColor) => {
    return text; // Simplificado: igual que arriba
};

export { color, bgcolor };
